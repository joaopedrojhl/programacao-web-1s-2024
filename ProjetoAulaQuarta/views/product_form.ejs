<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.id ? 'Editar Produto' : 'Novo Produto' %></title>
</head>
<body>
    <h1><%= product.id ? 'Editar Produto' : 'Novo Produto' %></h1>
    <form action="<%= product.id ? '/product/edit/' + product.id : '/product/new' %>" method="POST">
        <label for="name">Nome:</label>
        <input type="text" id="name" name="name" value="<%= product.name %>" required>
        <br>
        <label for="price">Preço:</label>
        <input type="number" step="0.01" id="price" name="price" value="<%= product.price %>" required>
        <br>
        <button type="submit"><%= product.id ? 'Atualizar' : 'Cadastrar' %></button>
    </form>
    <a href="/products">Voltar</a>
</body>
</html>
